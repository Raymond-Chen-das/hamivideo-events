# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.ipynb_checkpoints/
.pytest_cache/

# 備份檔（驗證通過即刪；萬一遺留也不進版控）
*.bak
*.bak-*

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/

# 注意：charts/plotly.min.js 是刻意納入版控的。
# 圖表必須能離線開啟，所以把 bundle 隨 repo 帶著，而不是從 CDN 載。
# 它不會變動，因此不會讓歷史膨脹。

# ── 原始快照不隨版控散布 ────────────────────────────────────────────
# data/raw/*.csv 由 pytrends 從 Google Trends 的非官方端點取得。
# 該服務條款對「再散布原始資料」的規範未經確認，因此不放進公開 repo。
# 圖表與摘要是靜態產出，不受影響，照常隨 repo 提供。
# 取得方式與配額限制見 README 的「原始資料的取得」。
data/raw/*.csv

# 內部工作文件（僅本機，不公開）
CLAUDE.md
docs/10-project-spec.md
docs/02-project-b-bootstrap.md
docs/decision-trail.md
docs/prompt-verify-google-trends.md
